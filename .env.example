# Gmail OAuth2 credentials
# Obtain via Google Cloud Console → APIs & Services → Credentials
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# The Gmail address that sends the briefing (must match the account you authorized)
GMAIL_SENDER_EMAIL=
